#include <stdio.h>
/*

int maxEleman(int A[],int eleman)
{
    int j,i,gecici;

    for(j=0;j<eleman;j++)
    {
        for(i=0;i<eleman;i++)
        {
            if(A[i+1]>A[i])
            {
                gecici = A[i];
                A[i]=A[i+1];
                A[i+1] = gecici ;
            }
        }
    }

        for(i=0;i<=eleman;i++)
        {
            printf("%d ",A[i]);
        }
}

int main() {

	int A[7] = {50, -20, 30, 70, -40, -10, 60};
	int B[7] = {5, 10, -5, 35, -10, 20, -15, 15, 25};

	printf("A dizisinde max eleman = %d\n", maxEleman(A,7));
	printf("B dizisinde max eleman = %d\n", maxEleman(B,9));

	*/

return 0;
}
